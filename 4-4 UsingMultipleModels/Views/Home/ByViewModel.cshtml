@model _4_4_UsingMultipleModels.Models.ViewDataModel // ต้องอ้างอิง Class


@{
    ViewData["Title"] = "ByViewModel";
}

    <div>
        <h4>ViewModel  / ViewDataModel</h4>
        <pre>
            เหมือนสร้าง class แล้ว ประกาศตัวแปร Class ไว้เก็บค่า
            ต้องรับค่ามาแสดงผ่าน method view
            ต้องเรียกใช้งาน ด้วย model ใน foreach
        </pre>
        <hr />
        <dl class="row">
        </dl>
    </div>

@*<div>
        @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        <a asp-action="Index">Back to List</a>
    </div>*@

<h1> แบบ ==> ByViewModel</h1>

<table border="1">
    <tr>
        <th>รหัสลูกค้า</th>
        <th>ชื่อ-สกุล</th>
        <th>ที่อยู่</th>

        @foreach (var item in Model.GetCustomers)
        {
        <tr>
            <td>@item.CustomerID</td>
            <td>@item.FullName</td>
            <td>@item.Address</td>
        </tr>
    }
        </tr>
    </table>
    <br />

    <table border="1">
        <tr>
            <th>รหัสสินค้า</th>
            <th>ชื่อสินค้า</th>
            <th>ราคา</th>
        </tr>

        @foreach (var item in Model.GetProducts)
        {
            <tr>
                <td>@item.ProductID</td>
                <td>@item.ProductName</td>
                <td>@item.ProductPrice</td>
            </tr>
        }
    </table>


